# AI Agent Instructions

## instrumented/ — DO NOT MODIFY

The `instrumented/` folder is **auto-generated by Simulink** from the corresponding source files in this `Src/` directory.

**Never edit, create, or delete files inside `instrumented/` directly.**

Any changes must be made to the original source file in `Core/Src/` and then propagated to the matching file in `instrumented/` as a separate, explicit step — only when the user asks for it.
